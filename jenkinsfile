
pipeline {
    agent any


    stages{
        stage("Login to the instance") {
            steps{
                ssh-agent (credentials: ['test123']){
                    sh 'echo "Hello World"'
                }
            }
        }
    }
}
